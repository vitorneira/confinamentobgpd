NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Orquestrador (docs/orquestrador/) — classificação e transcrição
ANTHROPIC_API_KEY=
GROQ_API_KEY=

# Orquestrador — Fase M3 (bot do Telegram)
TELEGRAM_BOT_TOKEN=
# Texto qualquer que você inventa — o bot manda de volta no header de cada
# update; a rota do webhook confere que bate antes de processar (ver
# src/app/api/telegram-webhook/route.ts).
TELEGRAM_WEBHOOK_SECRET=
